{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    }
  },
  "globalEnv": ["DATABASE_URL", "NEXT_PUBLIC_CLERK_FRONTEND_API", "CLERK_API_KEY", "CLERK_JWT_KEY"]
}
